# ============================================================
# Proactive AI Landing 环境变量
# 部署到 EdgeOne 时在平台环境中配置以下变量（构建时注入）
# ============================================================

# 必填：下载体验按钮跳转地址（腾讯云 COS/CDN 文件直链）
# 示例：NEXT_PUBLIC_DEMO_URL=https://cdn.example.com/dists/ProactiveAI-Setup-1.0.0.exe
NEXT_PUBLIC_DEMO_URL=

# 说明：联系表单固定 POST 到同域 /api/contact（EdgeOne 自动挂载 edge-functions/api/contact.js），
# 无需额外的 NEXT_PUBLIC_CONTACT_API 环境变量。

# ============================================================
# Edge Function（edge-functions/api/contact.js）侧变量：
# 部署 Edge Function 时在函数配置里填（非 NEXT_PUBLIC_*，不进前端）
# ============================================================
# SES_SECRET_ID=     腾讯云 SecretId（有 SES 权限）
# SES_SECRET_KEY=    腾讯云 SecretKey
# SES_REGION=        SES 区域，默认 ap-hongkong
# SES_FROM=          发件人邮箱（SES 已验证）
# SES_TO=            收件人邮箱（接收联系表单通知）
# SES_TEMPLATE_ID=   SES 邮件模板 ID（模板变量含 name/email/institution/message）
